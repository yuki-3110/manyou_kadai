<h1>タスク一覧</h1>

<table>
  <tr>
    <th>タスク名</th>
    <th>タスク詳細</th>
  </tr>
<% @tasks.each do |task| %>
  <tr>
    <td><%= task.title %></td>
    <td><%= task.content %></td>
  </tr>
<% end %>
</table>